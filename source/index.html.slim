---
pageable: true
per_page: 8
---


/ main
.l-main
  / articles
  .main-articles.autopagerize_page_element
    - page_articles.each_with_index do |article, i|
      article.article
        p.article-image
          == image_tag "#{article.data.image}", :"data-original" => "#{article.data.image}", :alt => "#{article.title}"

        div.article-info
          .info-wrapper
            h1.article-title
              == link_to article.title, article

            hr

            p.article-date
              small
                == article.date.strftime("%Y/%m/%d")

            p.article-tag tags


/ pager
- if paginate
  .l-pager(data-maxpage="#{num_pages}")
    - if prev_page
      p
        == link_to "Previous page", prev_page

    - if next_page
      p.pager-next
        == link_to "Next page", next_page, :rel => "next"


/ loader
.l-loader
  .loader
    p.loader-text
      | loading
      span .
      span .
      span .


/ about
.l-about
  .about
    .about-overlay
    .about-text
      article
        h1.text-title About
        ul
          li Animation Gif Wallpaper for SmartPhone.
          li Resolution is 640x1136. (Apple iPhone 5s ready)
          li Required Jailbreak.
          li #{link_to "Source: GitHub", "", :target => "_blank"}

        h2 Naka-No-Hito
        ul
          li #{link_to "RYO NAKAE", "", :target => "_blank"}
          li #{link_to "Twitter", "", :target => "_blank"}
          li #{link_to "Dribbble", "", :target => "_blank"}
          li #{link_to "JAYPEG", "", :target => "_blank"}
          li #{link_to "GitHub", "", :target => "_blank"}

      .about-close
        .close-border
          .close-border-left
          .close-border-right